if(ENABLE_AV_UTILS)
    add_subdirectory(audioflinger) 
endif(ENABLE_AV_UTILS)

if(ENABLE_AV_CODEC)
    add_subdirectory(mediacodec) 
    add_subdirectory(mediaresourcemanager)  
    add_subdirectory(audiopolicy) 
    add_subdirectory(mediaextractor) 
endif(ENABLE_AV_CODEC)
